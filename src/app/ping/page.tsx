export default function PingPage() {
  return (
    <main style={{ padding: "3rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>It works — deploy smoke test</h1>
      <p>If you can see this, the push → build → deploy pipeline is fine.</p>
    </main>
  );
}
